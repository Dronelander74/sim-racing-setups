Anything in `docs/acc/` is \*\*explicitly excluded\*\* from the runtime contract.



\---



\## Summary (Non‑Negotiable)



> Documentation explains \*\*how the game works\*\*.  

> Setups define \*\*what values are used\*\*.  

> These two concerns must never be mixed.

